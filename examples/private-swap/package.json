{
  "name": "@sip-protocol/example-private-swap",
  "version": "0.1.0",
  "private": true,
  "description": "Private cross-chain swap example using SIP Protocol",
  "main": "index.ts",
  "scripts": {
    "start": "ts-node index.ts",
    "start:live": "LIVE_QUOTES=true ts-node index.ts"
  },
  "dependencies": {
    "@sip-protocol/sdk": "workspace:*"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
